[project]
name = "generate-soal"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.0", "pydantic-settings>=2.0", "pymupdf>=1.24", "pillow>=10.0"]

[project.optional-dependencies]
test = ["pytest>=8.0"]
